config.stopBubbling = true

# @RequiredArgsConstructor가 생성자를 만들 때 필드의 @Value 어노테이션을
# 생성자 파라미터에 복사한다. 이 설정이 없으면 Spring이 @Value 프로퍼티를
# 주입하지 못해 UnsatisfiedDependencyException이 발생한다.
# 검증: LombokValueInjectionGuardTest
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Value
